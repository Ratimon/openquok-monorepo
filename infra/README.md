# Infrastructure

Compose and supporting files for services used by the monorepo but not shipped inside the app images.

- **[temporal](./temporal/)** — self-hosted Temporal (PostgreSQL + Elasticsearch + server + Web UI) for workflow development and private deployments.
