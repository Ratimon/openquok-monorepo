{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "sh -c \"if [ -f pnpm-lock.yaml ]; then pnpm install --frozen-lockfile --ignore-workspace --config.child-concurrency=1 --config.network-concurrency=1; else pnpm install --ignore-workspace --config.child-concurrency=1 --config.network-concurrency=1; fi\"",
  "buildCommand": "pnpm run build",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/api/$1" }]
}
