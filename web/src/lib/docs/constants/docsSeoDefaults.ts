/** Root-relative fallback when a doc has no `ogImage` and no inline screenshots. */
export const DOCS_FALLBACK_SOCIAL_IMAGE_SRC = '/pwa/favicon.svg';

export const DOCS_FALLBACK_SOCIAL_IMAGE_ALT = 'OpenQuok logo';
