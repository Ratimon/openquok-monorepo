/** Polotno SDK API origin (template list: `get-templates`). */
export const POLOTONO_API_ORIGIN = 'https://api.polotno.com';

export const POLOTONO_GET_TEMPLATES_PATH = '/api/get-templates';

/** Text-style templates (`get-text-templates`). */
export const POLOTONO_GET_TEXT_TEMPLATES_PATH = '/api/get-text-templates';

/** Unsplash search proxy (same contract as OpenPolotno `get-unsplash`). */
export const POLOTONO_GET_UNSPLASH_PATH = '/api/get-unsplash';

/** Unsplash download tracking when a photo is used (Polotno proxy). */
export const POLOTONO_DOWNLOAD_UNSPLASH_PATH = '/api/download-unsplash';

export const POLOTONO_DEFAULT_TEMPLATE_PER_PAGE = 30;

export const POLOTONO_DEFAULT_UNSPLASH_PER_PAGE = 20;
