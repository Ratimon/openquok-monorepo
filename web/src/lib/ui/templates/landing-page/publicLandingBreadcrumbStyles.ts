export const PUBLIC_LANDING_BREADCRUMB_LINK_CLASS =
	'text-primary hover:text-primary/80 text-sm font-bold uppercase tracking-wider no-underline';

export const PUBLIC_LANDING_BREADCRUMB_PAGE_CLASS =
	'text-primary line-clamp-1 text-sm font-bold uppercase tracking-wider';

export const PUBLIC_LANDING_BREADCRUMB_SEPARATOR_CLASS = 'text-primary/60';

/** Blog breadcrumbs — Title Case only (no `uppercase` transform). */
export const PUBLIC_BLOG_BREADCRUMB_LINK_CLASS =
	'text-primary hover:text-primary/80 text-sm font-bold tracking-wider no-underline';

export const PUBLIC_BLOG_BREADCRUMB_PAGE_CLASS = 'text-primary line-clamp-1 text-sm';
