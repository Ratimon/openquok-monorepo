/** Stage background grid spacing (stage pixels). */
export const GRID_STEP = 20;

/** Inset around the page rect in standalone (non-embedded) mode. */
export const PAGE_MARGIN = 28;

/** Tighter inset when the canvas is embedded in the design shell. */
export const PAGE_MARGIN_EMBEDDED = 8;
